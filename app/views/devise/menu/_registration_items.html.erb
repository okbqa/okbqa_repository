<% if user_signed_in? %>
  Welcome <%= link_to current_user.email, edit_user_registration_path %>
<% else %>
  <%= link_to 'Sign Up', new_user_registration_path, :class => 'capitalize_first' %>
<% end %>